# Bug 修复记录与改进建议

> 记录每次bug修复的原因、解决方案和改进点，防止类似问题再次发生

## 修复记录

### Bug #001 (2026-04-16)
**问题描述**: wei.tao@shopee.com 登录之后，仍显示"请求失败：需要登录"

**根本原因**: 
- 代码中token键名不一致：`changePassword`函数使用`localStorage.getItem('token')`，而其他地方使用`localStorage.getItem('access_token')`

**修复方案**:
- 统一将`changePassword`函数中的token获取改为`access_token`

**影响文件**:
- `frontend/src/api.js` (line 268)

**改进点**:
- ✅ 建立代码规范：所有localStorage键名使用统一常量定义
- ✅ 添加代码审查检查点：检查localStorage键名一致性
- ✅ 考虑创建统一的auth工具类管理token操作

**预防措施**:
```javascript
// 建议：定义常量避免硬编码
const TOKEN_KEY = 'access_token';
const USER_KEY = 'user';

// 统一的token操作函数
export const getAuthToken = () => localStorage.getItem(TOKEN_KEY);
export const setAuthToken = (token) => localStorage.setItem(TOKEN_KEY, token);
```

---

### Bug #002 (2026-04-16)
**问题描述**: 点击了pin的聊天会显示黑屏

**根本原因**:
- 数据结构不一致：置顶对话使用`messages`数组存储多条消息，普通对话使用`message`对象存储单条消息
- 前端ChatPanel组件只处理了`message`对象格式，未处理`messages`数组格式

**修复方案**:
- 在ChatPanel中添加数据结构兼容处理，支持两种数据格式

**影响文件**:
- `frontend/src/components/ChatPanel.jsx` (useEffect hook, lines 34-61)

**改进点**:
- ✅ 数据结构标准化：统一对话数据格式，避免不同场景下的结构差异
- ✅ 防御性编程：前端组件应该能优雅处理多种数据格式
- ✅ 类型检查：考虑使用TypeScript或PropTypes进行数据结构验证

**预防措施**:
```javascript
// 建议：定义统一的数据结构转换函数
const normalizeConversationData = (conversation) => {
  // 统一转换为messages数组格式
  if (conversation.messages) return conversation.messages;
  if (conversation.message) return [conversation.message];
  return [];
};
```

---

### Bug #003 (2026-04-16)
**问题描述**: 需要实现单个用户账户pin的消息最多3个，且pin的消息retention设置为30天，非pin的消息retention设置为7天

**根本原因**:
- 缺少业务规则实现：
  1. 没有pin消息数量限制
  2. 所有消息都使用相同的30天retention策略

**修复方案**:
1. 实现pin消息3个数量限制，超出时自动取消最旧的pin状态
2. 实现差异化retention策略：pin消息30天，普通消息7天

**影响文件**:
- `src/core/conversation_manager.py`:
  - `_clean_expired_conversations()` method
  - `_clean_old_conversations()` method  
  - `update_conversation()` method

**改进点**:
- ✅ 配置化管理：将业务规则（数量限制、retention天数）提取为配置项
- ✅ 策略模式：不同类型消息的retention策略应该可扩展
- ✅ 事务性操作：pin状态变更时的数量检查应该是原子操作

**预防措施**:
```python
# 建议：配置化业务规则
class ConversationConfig:
    MAX_PINNED_CONVERSATIONS = 3
    PINNED_RETENTION_DAYS = 30
    REGULAR_RETENTION_DAYS = 7
    MAX_REGULAR_CONVERSATIONS = 10

# 建议：抽象retention策略
class RetentionStrategy:
    def get_retention_days(self, conversation_type: str) -> int:
        # 根据类型返回对应的保留天数
```

---

### Bug #004 (2026-04-16)
**问题描述**: 新用户通过邮箱验证码完成注册后立刻使用，侧边栏「历史对话」报 HTTP 错误；随便发一条消息再退出重登后不再报错，但历史为空。

**根本原因**:
- `verifyRegistration` 与 `login` 行为不一致：登录成功后会将 `access_token` 与 `user` 写入 `localStorage`，而**验证注册成功**只把 `user` 交给 React 状态，**未持久化 JWT**。
- 依赖 `Authorization: Bearer <token>` 的接口（如 `GET /conversations`、`POST /ask/stream`）从 `localStorage` 取 token，注册后 token 缺失 → **401**；流式问答无法以当前用户身份保存对话 → 重登后历史仍为空。

**修复方案**:
- 在 `verifyRegistration` 解析后端 `Token` 响应后，与 `login` 相同地执行：
  - `localStorage.setItem('access_token', data.access_token)`
  - `localStorage.setItem('user', JSON.stringify(data.user))`

**影响文件**:
- `frontend/src/api.js` — `verifyRegistration` 函数

**改进点**:
- ✅ 所有返回 `Token` 的认证接口（登录、验证注册、若仍使用 `POST /auth/register`）应**统一走同一套持久化**（或抽成 `persistAuthSession(data)`），避免遗漏。
- ✅ 与 Bug #001 一致：建议用 `TOKEN_KEY` / `USER_KEY` 常量或 `auth` 工具封装，避免多处复制粘贴。

**预防措施**:
```javascript
// 建议：登录/注册成功后统一调用
function persistAuthSession(data) {
  localStorage.setItem('access_token', data.access_token);
  localStorage.setItem('user', JSON.stringify(data.user));
}
// login、verifyRegistration、（若使用的）register 均调用 persistAuthSession
```

---

### 配置更新 #001 (2026-04-16)
**更改内容**: 启用真实邮件发送功能

**修改详情**:
- 将 `src/core/email_service.py` 中的 `development_mode` 从 `True` 改为 `False`
- 现在系统可以发送真实邮件进行密码重置和用户注册验证

**影响功能**:
- 忘记密码功能：发送真实重置验证码邮件
- 用户注册功能：发送真实注册验证码邮件
- 欢迎邮件：新用户注册成功后发送欢迎邮件

**邮件服务配置**:
- SMTP服务器: `smtp.126.com:465` (SSL)
- 发送邮箱: `Tagent_official@126.com`
- 认证: 126邮箱客户端授权密码

**注意事项**:
- 邮件发送需要网络连接
- 126邮箱需要预先开启SMTP服务并设置客户端授权密码
- 生产环境建议将邮箱密码通过环境变量管理而非硬编码

---

## 通用改进建议

### 代码质量
1. **常量定义**: 避免魔法数字和硬编码字符串
2. **类型安全**: 考虑引入TypeScript增强类型检查
3. **错误处理**: 增加更完善的错误边界和降级处理
4. **单元测试**: 为关键业务逻辑添加测试覆盖

### 开发流程
1. **Code Review**: 重点检查数据结构一致性和业务规则实现
2. **文档维护**: 及时更新API文档和数据结构说明
3. **配置管理**: 业务规则应该通过配置文件管理，而非硬编码

### 架构设计
1. **数据一致性**: 建立统一的数据模型和转换规范
2. **业务规则**: 重要业务逻辑应该集中管理，避免分散实现
3. **可扩展性**: 设计时考虑未来可能的变更需求

---

## Bug记录模板

```
### Bug #XXX (YYYY-MM-DD)
**问题描述**: 

**根本原因**: 

**修复方案**: 

**影响文件**: 

**改进点**: 

**预防措施**: 

---
```

## 下次提bug时的检查清单

- [ ] 新认证流程（注册/验证码/第三方登录）是否在成功后与 `login` 一样持久化 `access_token` / `user`？
- [ ] 是否是数据结构不一致导致的问题？
- [ ] 是否是硬编码常量不统一？
- [ ] 是否是业务规则缺失或不完整？
- [ ] 修复是否可能引入新的问题？
- [ ] 是否需要更新相关文档？
- [ ] 是否需要添加测试用例？